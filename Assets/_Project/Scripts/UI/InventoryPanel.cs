using UnityEngine;

/// <summary>
/// View component for the inventory panel (scroll/list, selection highlight).
/// Hook this GameObject to InventoryUIController; no business logic.
/// </summary>
public class InventoryPanel : MonoBehaviour
{
    // Purely view: bind this root to InventoryUIController.panelRoot and listContainer in Inspector.
}
